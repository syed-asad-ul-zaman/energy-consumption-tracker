export const metadata = {
    title: 'Measurements',
    description: 'Measurements CRUD',
};

export default function MeasurementsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>
            {children}
        </>
    );
}
